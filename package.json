{
  "name": "hyperworker",
  "version": "1.0.0",
  "description": "Always-on multi-tenant trailing stop-loss engine for Hyperliquid",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nktkas/hyperliquid": "^0.33.2",
    "@upstash/redis": "^1.38.0",
    "pino": "^10.3.1",
    "viem": "^2.55.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
